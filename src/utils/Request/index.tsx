import { useRequest } from 'umi';
import request,{requestProvide,pageRequest} from './request';

export default {
    requestProvide,
    useRequest,
    request,
    pageRequest
};
export {
    pageRequest,
    requestProvide,
    useRequest,
    request
};
